pre-commit:
  parallel: true
  commands:
    eslint:
      glob: 'src/*.{ts,tsx}'
      run: pnpm lint --fix {staged_files}
    prettier:
      glob: 'src/*'
      run: pnpm format --write {staged_files}
